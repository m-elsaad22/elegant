# إعادة كتابة المقالات — 9 سبتمبر 2026

تم التنفيذ على https://elegantswimmingpools.com عبر REST (`elegant/v1/apply`) + محرّك توليد فريد.

## المشكلة
- نحو **1973** مقالًا منشورًا.
- صفحات المدن/الخدمات كانت قالبًا واحدًا (~650–700 كلمة): «لماذا نحن؟ / الخطوات / تواصل».
- نحو 111 عنوانًا مكررًا.
- 9 مقالات خارج نشاط المسابح (جبس، ديكور، حدائق، تسربات عامة، عزل أسطح، تسليك، تنظيف منازل).
- الشورت كود والحقول أسفل المقال (FAQ، المميزات، الخطوات، الخدمات، الأسعار، Rank Math، الوسوم، المدن) فارغة في الغالب.

## ما يفعله المحرّك لكل مقال
- محتوى عربي جديد **2500+ كلمة** بهيكل مختلف حسب `post_id` (12 تخطيطًا + زاوية تقنية من 9).
- عنوان فريد لكل تكرار لنفس الخدمة/المدينة.
- بلوكات بصرية متجاوبة بألوان الموقع (`--navy --turq --wa --bg`).
- شورت كود داخل المحتوى: `[post_features] [post_steps] [post_services] [post_prices] [post_call]`
- ملء ميتا القالب: الأسئلة الشائعة، المميزات، خطوات العمل، الخدمات، جدول عوامل التكلفة (بدون أسعار مخترعة)، بطاقة الطلب، البوب أب، سكيما Service/Article.
- Rank Math: عنوان، وصف، كلمة تركيز.
- وسوم + تصنيف `cities`.
- واتساب فقط `201556644443` — أزرار الاتصال مخفية.
- المقالات الخارجة عن النشاط تُحوَّل إلى خدمة مسابح مرتبطة (تشطيب المحيط، تسرب الحوض، تنسيق حديقة المسبح، عزل، صرف) مع الإبقاء على الـ slug.

## الملفات
- `elegant-articles-apply.php` — مقتطف Code Snippets رقم 6
- `elegant_article_gen.py` — توليد المحتوى
- `run_rewrite.py` — الدفعات عبر REST

## الاستكمال
الاستضافة (LiteSpeed) تتوقف إذا كُتبت مئات المقالات بسرعة. المحرّك يتخطى المنجز. لاستكمال الباقي:

`python3 docs/audit/run_rewrite.py`

## التحقق
- طنطا تنظيف: جداول + شورت كود + FAQ + واتساب 201556644443 (سطح مكتب/هاتف)
- تكلفة البناء في مدينة نصر: هيكل مختلف
- صفحة الجبس بالمنصورة: صارت تشطيب محيط المسبح
